"""Learner-centered coaching path — runs alongside the existing skill_graph/
planner path (see HANDOFF.md), does not replace it. The learner is the unit
of analysis here, not any single repository."""
